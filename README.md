# Лабораторная работа 4.2 Обработка стандартных исключений в Python
## Цель
Научиться понимать природу и разновидности ошибок, возникающих в программном обеспечении, а также исследовать подходы к их поиску, отладке и обработке. Освоить обработку исключений в Python и научиться писать надёжный код с минимизацией ошибок.
## Вариант 3
Исходный код:
```
def first_vacant_row(seats):
    """Вернуть первый ряд, в котором имеется больше всего
    свободных мест и их количество.

    Возвращаемая нумерация рядов с 1. Если свободных мест нет, вернуть 0, 0.

    Параметры:
        - seats (list of list): информация о проданных билетах
                                (1 - продано, 0 - нет).

    Результат:
        - tuple (ряд, количество мест).
    """
    max_count = 0
    max_row = 0
    for row_index, row in enumerate(seats):
        available_seats_count = row.count(0)  # 0 - пусто
        if available_seats_count >= max_count:
            max_row = row_index
            max_count = available_seats_count

    return max_row, max_count
```
Исправленный код:
```
"""
Ошибки (номера строк через пробел, данная строка - №2): 11
"""
def first_vacant_row(seats):
    max_count = 0
    max_row = 0
    for row_index, row in enumerate(seats):
        available_seats_count = row.count(0)  # 0 - пусто
        if available_seats_count >= max_count:
            max_row = row_index
            max_count = available_seats_count

    return max_row + 1, max_count  # Исправлено: +1 для нумерации рядов с 1
```
## Вывод
В ходе данной лабораторной работы мы научились понимать природу и разновидности ошибок, возникающих в программном обеспечении, а также исследовать подходы к их поиску, отладке и обработке, кроме того мы освоили обработку исключений в Python и научиться писать надёжный код с минимизацией ошибок.
